<nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top">
  <a class="navbar-brand" href="{{ path('home') }}">

    <img src="/img/book.png" width="30" height="30" class="d-inline-block align-top" alt="">
    Help Student
  </a>
  <div class="collapse navbar-collapse" id="navbarColor01">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="{{ path('accueil') }}">Accueil</a>
      </li>
      {% if app.user %}
      <li class="nav-item ">
        <a class="nav-link" href="{{ path('profil') }}">Profil</a>
      </li>
      {% endif %}
      <li class="nav-item">
        <a class="nav-link" href="#">About</a>
      </li>
    </ul>
    {% if is_granted('ROLE_ADMIN') %}
    <a href="{{path('admin_view')}}" class="btn btn-secondary my-2 my-sm-0">Administration</a>&nbsp;
    {% endif %}

    {% if not app.user %}
    <a href="{{ path('security_login') }}" class="btn btn-secondary my-2 my-sm-0">Login</a>&nbsp;
    <a href="{{ path('security_registration') }}" class="btn btn-warning my-2 my-sm-0">Sign Up</a>

    {% else %}
    <a href="{{ path('security_logout') }}" class="btn btn-warning my-2 my-sm-0">Logout</a>
    <a href="{{ path('create') }}" class="btn btn-info my-2 my-sm-0">Need help</a>&nbsp;

    {% endif %}

  </div>
</nav>

<br><br>